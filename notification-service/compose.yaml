services:
    php: &php_service
        container_name: notification-service
        image: ${IMAGES_PREFIX:-}app-php
        restart: unless-stopped
        environment:
            SERVER_NAME: ${SERVER_NAME:-localhost}, php:80
            MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
            MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
            # Run "composer require symfony/orm-pack" to install and configure Doctrine ORM
            DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/${POSTGRES_DB:-app}?serverVersion=${POSTGRES_VERSION:-15}&charset=${POSTGRES_CHARSET:-utf8}
            # Run "composer require symfony/mercure-bundle" to install and configure the Mercure integration
            MERCURE_URL: ${CADDY_MERCURE_URL:-http://php/.well-known/mercure}
            MERCURE_PUBLIC_URL: ${CADDY_MERCURE_PUBLIC_URL:-https://${SERVER_NAME:-localhost}:${HTTPS_PORT:-443}/.well-known/mercure}
            MERCURE_JWT_SECRET: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
            # The two next lines can be removed after initial installation
            SYMFONY_VERSION: ${SYMFONY_VERSION:-}
            STABILITY: ${STABILITY:-stable}
        networks:
            - employee-network
        volumes:
            - ./:/app
            - caddy_data:/data
            - caddy_config:/config
        ports:
            # HTTP
            - target: 80
              published: ${HTTP_PORT:-82}
              protocol: tcp
            # HTTPS
            - target: 443
              published: ${HTTPS_PORT:-8442}
              protocol: tcp
            # HTTP/3
            - target: 443
              published: ${HTTP3_PORT:-8442}
              protocol: udp

    #https://github.com/dunglas/symfony-docker/issues/539
    messenger-consumer:
        <<: *php_service
        container_name: ''
        restart: unless-stopped
        deploy:
            mode: replicated
            replicas: 2
        healthcheck:
            disable: true
            #command: /usr/bin/true # if "disable: true" isn't works

        # if you need to disable the entry point provided by this repository
        #entrypoint: ""

        # https://symfony.com/doc/current/messenger.html#deploying-to-production
        command: bin/console messenger:consume welcome_email --time-limit=3600
        ports: []

# Mercure is installed as a Caddy module, prevent the Flex recipe from installing another service
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

networks:
    employee-network:
        external: true

volumes:
    caddy_data:
    caddy_config:
        ###> symfony/mercure-bundle ###
        ###< symfony/mercure-bundle ###
